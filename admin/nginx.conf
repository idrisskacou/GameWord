; server {
;     listen 80;
;     server_name _;  # Match any hostname

;     root /usr/share/nginx/html;
;     index index.html;

;     location / {
;         try_files $uri $uri/ /index.html;
;     }

;     # Optional: Set caching headers for static assets
;     location ~* \.(js|css|png|jpg|jpeg|gif|svg|ico)$ {
;         expires 1h;
;         add_header Cache-Control "public";
;     }

;     # Security headers
;     add_header X-Frame-Options "SAMEORIGIN" always;
;     add_header X-Content-Type-Options "nosniff" always;
;     add_header X-XSS-Protection "1; mode=block" always;
; }

server {
    listen 80;
    server_name idriss.com;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Access and error logs
    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;

    # Optional: Define log format
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    # Static assets caching
    location ~* \.(js|css|png|jpg|jpeg|gif|svg|ico)$ {
        expires 1h;
        add_header Cache-Control "public";

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    }
}

# server {
#     listen 80;
#     server_name localhost;

#     root /usr/share/nginx/html;
#     index index.html;

#     location / {
#         try_files $uri $uri/ /index.html =404;
#     }

#     # Proxy requests to the SSR server (Node.js)
#     location /api {
#         proxy_pass http://node:4000;  # Assuming Node.js SSR server runs on port 4000
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
# Main nginx configuration file

; worker_processes auto;
; events { worker_connections 1024; }

; http {
;     include       /etc/nginx/mime.types;
;     default_type  application/octet-stream;
;     access_log    /var/log/nginx/access.log;
;     error_log     /var/log/nginx/error.log;

;     server {
;         listen 80;
;         server_name localhost;

;         # Set the root to the Angular app's output directory
;         root /usr/share/nginx/html;  # This should be the location where your build files are copied
;         index index.html;

;         # Handle routing for Angular Single Page Application (SPA)
;         location / {
;             try_files $uri $uri/ /index.html;  # Fallback to index.html for Angular SPA routing
;         }

;         # Optionally, you can define locations for specific parts of your app, like `server` or `browser` subfolders.
;         location /server/ {
;             root /usr/share/nginx/html;  # Adjust if you have a specific build path for "server"
;             try_files $uri $uri/ /server/index.html;
;         }

;         location /browser/ {
;             root /usr/share/nginx/html;  # Adjust if you have a specific build path for "browser"
;             try_files $uri $uri/ /browser/index.html;
;         }
;     }
; }
